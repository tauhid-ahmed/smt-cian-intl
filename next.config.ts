import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    images: {
        domains: [
            "images.unsplash.com",
            "picsum.photos",
            "m.media-amazon.com",
            "206.162.244.175",
        ],
    },
};

export default nextConfig;
